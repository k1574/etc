#/bin/sh

menu="$XFALLMENU"

case "`basename $menu`" in
	9menu )
		cd "$SCRIPTS"
		scripts=*
		fullscripts=""
		for i in $scripts ; do
			fullscripts="$fullscripts $i:./$i"
		done
		9menu -teleport -popup $fullscripts 
	;;
esac
